label Provide the answers below

ul.list-unstyled
  li [ng-repeat="letter in collection.selective_letters track by $index"]

    p.wrap-input

      span.choice-label  {{$index + 1}} .
      input.form-control [ng-model="model.question_answers[$index]" type="text"]

